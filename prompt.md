# Промпт для Cursor: Telegram Dating Bot для университетов

```
Создай полноценного Telegram бота для знакомств внутри университетов на Python + PostgreSQL. Бот должен полностью повторять функционал бота "Леонардо Дайвинчик", но с добавлением привязки к университетам.

## ТЕХНИЧЕСКИЙ СТЕК

- Python 3.11+
- aiogram 3.x (последняя версия)
- PostgreSQL + asyncpg
- SQLAlchemy 2.0 (async)
- Alembic для миграций
- Redis для FSM storage (опционально, можно MemoryStorage для простоты)
- python-dotenv для переменных окружения
- Структура проекта должна быть модульной и чистой

## СТРУКТУРА ПРОЕКТА

```
university_dating_bot/
├── bot.py                      # Точка входа
├── .env                        # BOT_TOKEN, ADMIN_ID, DATABASE_URL
├── .env.example
├── requirements.txt
├── alembic.ini
├── alembic/
│   └── versions/
├── app/
│   ├── __init__.py
│   ├── config.py               # Загрузка конфигурации из .env
│   ├── database/
│   │   ├── __init__.py
│   │   ├── engine.py           # Создание async engine и session
│   │   ├── models.py           # Все SQLAlchemy модели
│   │   └── repositories/       # Репозитории для работы с БД
│   │       ├── __init__.py
│   │       ├── user_repo.py
│   │       ├── university_repo.py
│   │       ├── like_repo.py
│   │       ├── match_repo.py
│   │       └── report_repo.py
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── start.py            # /start команда
│   │   ├── registration.py     # Регистрация анкеты
│   │   ├── profile.py          # Управление профилем
│   │   ├── viewing.py          # Просмотр анкет
│   │   ├── likes.py            # Обработка лайков
│   │   ├── matches.py          # Взаимные симпатии
│   │   ├── messages.py         # Сообщения при лайке
│   │   ├── reports.py          # Жалобы
│   │   └── admin.py            # Админ панель
│   ├── keyboards/
│   │   ├── __init__.py
│   │   ├── inline.py           # Inline клавиатуры
│   │   └── reply.py            # Reply клавиатуры
│   ├── states/
│   │   ├── __init__.py
│   │   └── states.py           # FSM состояния
│   ├── middlewares/
│   │   ├── __init__.py
│   │   └── db_middleware.py    # Middleware для сессии БД
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── text_templates.py   # Все тексты бота
│   │   └── helpers.py          # Вспомогательные функции
│   └── services/
│       ├── __init__.py
│       ├── matching_service.py # Логика подбора анкет
│       └── notification_service.py # Уведомления
```

## МОДЕЛИ БАЗЫ ДАННЫХ (SQLAlchemy)

### 1. University (Университеты)
```python
class University(Base):
    __tablename__ = "universities"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(255), unique=True)  # "МГУ", "МГТУ им. Баумана"
    short_name: Mapped[str] = mapped_column(String(50))  # "МГУ", "Бауманка"
    city: Mapped[str] = mapped_column(String(100))  # Город
    is_active: Mapped[bool] = mapped_column(default=True)  # Активен ли
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    
    users = relationship("User", back_populates="university")
```

### 2. User (Пользователи/Анкеты)
```python
class User(Base):
    __tablename__ = "users"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    telegram_id: Mapped[int] = mapped_column(BigInteger, unique=True, index=True)
    username: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    
    # Данные анкеты
    name: Mapped[str] = mapped_column(String(100))  # Имя
    age: Mapped[int] = mapped_column(Integer)  # Возраст
    gender: Mapped[str] = mapped_column(String(10))  # "male" / "female"
    looking_for: Mapped[str] = mapped_column(String(10))  # "male" / "female" / "any"
    bio: Mapped[str] = mapped_column(Text)  # Описание "о себе"
    
    # Медиа (до 3 фото)
    photo_1: Mapped[str] = mapped_column(String(255))  # file_id главного фото
    photo_2: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    photo_3: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    
    # Привязка к университету
    university_id: Mapped[int] = mapped_column(ForeignKey("universities.id"))
    university = relationship("University", back_populates="users")
    
    # Статусы
    is_active: Mapped[bool] = mapped_column(default=True)  # Анкета активна
    is_banned: Mapped[bool] = mapped_column(default=False)  # Забанен
    is_registered: Mapped[bool] = mapped_column(default=False)  # Прошел регистрацию
    
    # Настройки
    show_in_search: Mapped[bool] = mapped_column(default=True)  # Показывать в поиске
    
    # Временные метки
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    updated_at: Mapped[datetime] = mapped_column(default=datetime.utcnow, onupdate=datetime.utcnow)
    last_active: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    
    # Связи
    sent_likes = relationship("Like", foreign_keys="Like.from_user_id", back_populates="from_user")
    received_likes = relationship("Like", foreign_keys="Like.to_user_id", back_populates="to_user")
    reports_sent = relationship("Report", foreign_keys="Report.from_user_id")
    reports_received = relationship("Report", foreign_keys="Report.to_user_id")
```

### 3. Like (Лайки)
```python
class Like(Base):
    __tablename__ = "likes"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    from_user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    to_user_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    
    message: Mapped[Optional[str]] = mapped_column(Text, nullable=True)  # Сообщение при лайке
    is_like: Mapped[bool] = mapped_column(Boolean)  # True = лайк, False = дизлайк
    
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    
    from_user = relationship("User", foreign_keys=[from_user_id], back_populates="sent_likes")
    to_user = relationship("User", foreign_keys=[to_user_id], back_populates="received_likes")
    
    __table_args__ = (
        UniqueConstraint('from_user_id', 'to_user_id', name='unique_like'),
    )
```

### 4. Match (Взаимные симпатии)
```python
class Match(Base):
    __tablename__ = "matches"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    user1_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    user2_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    
    is_active: Mapped[bool] = mapped_column(default=True)  # Мэтч активен
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    
    user1 = relationship("User", foreign_keys=[user1_id])
    user2 = relationship("User", foreign_keys=[user2_id])
```

### 5. Report (Жалобы)
```python
class Report(Base):
    __tablename__ = "reports"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    from_user_id: Mapped[int] = mapped_column(ForeignKey("users.id"))
    to_user_id: Mapped[int] = mapped_column(ForeignKey("users.id"))
    
    reason: Mapped[str] = mapped_column(String(50))  # Причина жалобы
    comment: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    
    status: Mapped[str] = mapped_column(String(20), default="pending")  # pending/reviewed/rejected
    admin_comment: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    reviewed_at: Mapped[Optional[datetime]] = mapped_column(nullable=True)
```

### 6. ViewedProfile (Просмотренные анкеты)
```python
class ViewedProfile(Base):
    __tablename__ = "viewed_profiles"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    viewer_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    viewed_id: Mapped[int] = mapped_column(ForeignKey("users.id"), index=True)
    created_at: Mapped[datetime] = mapped_column(default=datetime.utcnow)
    
    __table_args__ = (
        UniqueConstraint('viewer_id', 'viewed_id', name='unique_view'),
    )
```

## FSM СОСТОЯНИЯ

```python
from aiogram.fsm.state import State, StatesGroup

class RegistrationStates(StatesGroup):
    waiting_for_university = State()      # Выбор университета
    waiting_for_name = State()            # Ввод имени
    waiting_for_age = State()             # Ввод возраста
    waiting_for_gender = State()          # Выбор пола
    waiting_for_looking_for = State()     # Кого ищем
    waiting_for_photo = State()           # Загрузка фото (до 3)
    waiting_for_bio = State()             # Описание о себе
    confirm_profile = State()             # Подтверждение анкеты

class EditProfileStates(StatesGroup):
    choosing_what_to_edit = State()
    editing_name = State()
    editing_age = State()
    editing_gender = State()
    editing_looking_for = State()
    editing_photo = State()
    editing_bio = State()
    editing_university = State()

class ViewingStates(StatesGroup):
    viewing_profiles = State()            # Просмотр анкет
    writing_message = State()             # Написание сообщения при лайке

class ReportStates(StatesGroup):
    choosing_reason = State()
    writing_comment = State()

class AdminStates(StatesGroup):
    main_menu = State()
    adding_university = State()
    editing_university = State()
    viewing_reports = State()
    banning_user = State()
    broadcast_message = State()
```

## ПОЛНЫЙ ФУНКЦИОНАЛ БОТА

### КОМАНДА /start

1. Проверить, есть ли пользователь в БД
2. Если новый пользователь:
   - Отправить приветственное сообщение с эмодзи 💜
   - Текст: "Привет! 👋\n\nЯ бот для знакомств в твоём университете! 🎓\n\nЗдесь ты можешь найти новых друзей, единомышленников или даже свою любовь среди студентов твоего вуза 💕\n\nДавай создадим твою анкету!"
   - Кнопка: "Создать анкету 💫"
3. Если пользователь уже зарегистрирован:
   - Показать главное меню

### РЕГИСТРАЦИЯ (пошаговая)

**Шаг 1: Выбор университета**
```
Сообщение: "🎓 Выбери свой университет из списка:"

Inline клавиатура с университетами (пагинация по 5 штук):
[МГУ им. М.В. Ломоносова]
[МГТУ им. Н.Э. Баумана]
[НИУ ВШЭ]
[МФТИ]
[МИФИ]
[◀️ Назад] [Вперёд ▶️]
```

**Шаг 2: Имя**
```
Сообщение: "Как тебя зовут? 😊\n\nНапиши своё имя (как хочешь, чтобы тебя видели другие)"

Валидация: 2-50 символов, только буквы и пробелы
```

**Шаг 3: Возраст**
```
Сообщение: "Сколько тебе лет? 🎂\n\nНапиши число от 16 до 99"

Валидация: число от 16 до 99
Ошибка: "❌ Пожалуйста, введи корректный возраст (от 16 до 99)"
```

**Шаг 4: Пол**
```
Сообщение: "Укажи свой пол:"

Reply клавиатура:
[Я парень 👨]
[Я девушка 👩]
```

**Шаг 5: Кого ищешь**
```
Сообщение: "Кого ты хочешь найти? 💕"

Reply клавиатура:
[Парней 👨]
[Девушек 👩]
[Без разницы 🤷]
```

**Шаг 6: Фото**
```
Сообщение: "📸 Отправь свои фото (от 1 до 3 штук)\n\nПервое фото будет главным в анкете.\n\nКогда закончишь, нажми кнопку ниже."

Reply клавиатура:
[Готово ✅] (появляется после загрузки хотя бы 1 фото)

При загрузке каждого фото: "✅ Фото {n} добавлено! (всего: {n}/3)"
После 3 фото: "Максимум фото загружено! Нажми 'Готово ✅'"
```

**Шаг 7: О себе**
```
Сообщение: "Расскажи о себе ✨\n\nНапиши пару слов: чем увлекаешься, что любишь, какой ты человек.\n\nМаксимум 500 символов."

Reply клавиатура:
[Пропустить ⏭️]

Валидация: до 500 символов
```

**Шаг 8: Подтверждение**
```
Сообщение: "Отлично! Вот твоя анкета 👇"

[Отправить анкету с фото и данными - см. формат анкеты ниже]

Сообщение: "Всё верно?"

Reply клавиатура:
[Да, всё супер! ✅]
[Заполнить заново 🔄]
```

### ФОРМАТ ОТОБРАЖЕНИЯ АНКЕТЫ

```
[ФОТО - первое фото пользователя]

{Имя}, {Возраст}, {Университет} 🎓

{Текст о себе}
```

Пример:
```
[ФОТО]

Анна, 20, МГУ 🎓

Люблю читать книги, гулять по паркам и слушать инди-музыку. Ищу интересного собеседника 💫
```

Если несколько фото - отправлять как медиагруппу (альбом).

### ГЛАВНОЕ МЕНЮ (после регистрации)

```
Сообщение: "Что хочешь сделать? 💜"

Reply клавиатура (2x2 + 1):
[1. Смотреть анкеты 👀] [2. Моя анкета 📝]
[3. Я нравлюсь 💌]       [4. Взаимные 💕]
[5. Выключить анкету 😴]
```

### 1. СМОТРЕТЬ АНКЕТЫ (Viewing)

**Логика подбора анкет:**
1. Только пользователи из ТОГО ЖЕ университета
2. Противоположный пол (или любой, если "без разницы")
3. Учитывать looking_for обоих пользователей
4. Исключить уже просмотренные
5. Исключить забаненных и неактивных
6. Сортировка: сначала те, кто недавно был онлайн

**Интерфейс просмотра:**
```
[Показать анкету - фото + текст]

Inline клавиатура под анкетой:
[❤️] [👎] [💌] [💤] [⚠️]
```

Значения кнопок:
- ❤️ - Лайк
- 👎 - Дизлайк (пропустить)
- 💌 - Лайк с сообщением
- 💤 - Вернуться в меню (заснуть)
- ⚠️ - Пожаловаться

**При нажатии ❤️ (лайк):**
1. Сохранить лайк в БД
2. Проверить, есть ли взаимный лайк
3. Если есть взаимный лайк:
   - Создать Match
   - Отправить обоим уведомление о взаимной симпатии
4. Показать следующую анкету

**При нажатии 💌 (лайк с сообщением):**
```
Сообщение: "💌 Напиши сообщение для этого человека (до 200 символов):\n\nОно будет показано вместе с твоей анкетой."

Reply клавиатура:
[Отмена ❌]
```

После отправки сообщения - как обычный лайк, но с сохранением сообщения.

**При нажатии 👎 (дизлайк):**
1. Сохранить как дизлайк (is_like=False)
2. Показать следующую анкету

**При нажатии ⚠️ (жалоба):**
```
Сообщение: "Выбери причину жалобы:"

Inline клавиатура:
[Фото не соответствует]
[Оскорбительный контент]
[Продажа/реклама]
[Несовершеннолетний]
[Другое]
[Отмена ❌]
```

После выбора причины:
```
Сообщение: "Хочешь добавить комментарий к жалобе?"

Reply клавиатура:
[Пропустить]
```

После жалобы:
```
Сообщение: "✅ Жалоба отправлена. Мы проверим эту анкету.\n\nПоказать следующую анкету?"

Inline клавиатура:
[Продолжить 👀]
[В меню 🏠]
```

**Когда анкеты закончились:**
```
Сообщение: "🔍 Анкеты в твоём университете закончились!\n\nПопробуй зайти позже — здесь появляются новые люди каждый день 💫"

Reply клавиатура:
[Вернуться в меню 🏠]
```

### 2. МОЯ АНКЕТА (Profile)

```
[Показать свою анкету]

Сообщение: "Твоя анкета 👆"

Reply клавиатура:
[Редактировать ✏️]
[Изменить фото 📸]
[Изменить университет 🎓]
[Сбросить просмотры 🔄]
[Вернуться в меню 🏠]
```

**Редактировать:**
```
Сообщение: "Что хочешь изменить?"

Inline клавиатура:
[Имя]
[Возраст]
[О себе]
[Кого ищу]
[Назад ◀️]
```

**Изменить фото:**
```
Сообщение: "Отправь новые фото (от 1 до 3).\nТекущие фото будут заменены."

Reply клавиатура:
[Отмена ❌]
[Готово ✅] (после загрузки хотя бы 1 фото)
```

**Изменить университет:**
```
Сообщение: "⚠️ Если сменишь университет, тебе будут показываться анкеты только из нового вуза.\n\nВыбери новый университет:"

[Список университетов]
```

**Сбросить просмотры:**
```
Сообщение: "🔄 Просмотренные анкеты сброшены!\n\nТеперь тебе снова будут показываться все анкеты твоего университета."
```

### 3. Я НРАВЛЮСЬ (входящие лайки)

**Если есть непросмотренные лайки:**
```
Сообщение: "💌 Тебе нравятся {N} человек!\n\nСмотрим?"

Показать анкету первого лайкнувшего.

Если лайк был с сообщением:
"💌 Сообщение: {текст сообщения}"

Inline клавиатура:
[❤️] [👎] [💤]
```

- ❤️ - взаимный лайк (создаётся match)
- 👎 - отклонить
- 💤 - вернуться в меню

**Если нет лайков:**
```
Сообщение: "💔 Пока никто не оценил твою анкету.\n\nНо не расстраивайся — продолжай просматривать анкеты, и симпатии обязательно появятся! 💫"

Reply клавиатура:
[Смотреть анкеты 👀]
[В меню 🏠]
```

### 4. ВЗАИМНЫЕ СИМПАТИИ (Matches)

**Если есть мэтчи:**
```
Сообщение: "💕 Твои взаимные симпатии ({N}):"

Показать список (или по одному с пагинацией):

1. Анна, 20
@username или "Написать в Telegram"

2. Мария, 21
@username

Inline клавиатура под каждым:
[Написать 💬] [Удалить ❌]
```

Кнопка "Написать" - открывает ссылку на профиль (t.me/username) или показывает "Начни диалог: @username"

**Если нет мэтчей:**
```
Сообщение: "💔 У тебя пока нет взаимных симпатий.\n\nПродолжай просматривать анкеты — твоя пара может быть следующей! 💫"
```

### 5. ВЫКЛЮЧИТЬ/ВКЛЮЧИТЬ АНКЕТУ

**Если анкета активна:**
```
Сообщение: "😴 Твоя анкета скрыта.\n\nТебя больше не будут видеть другие пользователи, но ты по-прежнему можешь просматривать анкеты."

Кнопка меняется на: [Включить анкету 💫]
```

**Если анкета выключена:**
```
Сообщение: "💫 Твоя анкета снова активна!\n\nТеперь другие пользователи смогут тебя видеть."

Кнопка меняется на: [Выключить анкету 😴]
```

### УВЕДОМЛЕНИЕ О МЭТЧЕ

Когда происходит взаимный лайк, оба пользователя получают:

```
🎉 Есть взаимная симпатия!

[Показать анкету того, с кем мэтч]

Не стесняйся написать первым 💬

Telegram: @username

(Если нет username: "У этого пользователя скрыт username. Дождись, пока он/она напишет тебе первым!")
```

### УВЕДОМЛЕНИЕ О ЛАЙКЕ (опционально)

Можно отправлять пользователю уведомление, когда его лайкнули:

```
💌 Кто-то оценил твою анкету!

Зайди в раздел "Я нравлюсь", чтобы узнать кто 👀
```

---

## АДМИН ПАНЕЛЬ (/admin)

Доступна только для ADMIN_ID из .env

### Главное меню админа:

```
Сообщение: "👑 Админ-панель"

Inline клавиатура:
[📊 Статистика]
[🎓 Университеты]
[📋 Жалобы ({N} новых)]
[🚫 Забанить пользователя]
[📣 Рассылка]
[👥 Пользователи]
```

### 📊 Статистика:
```
📊 Статистика бота:

👥 Всего пользователей: {N}
✅ Активных анкет: {N}
😴 Неактивных: {N}
🚫 Забаненных: {N}

🎓 Университетов: {N}

💕 Всего мэтчей: {N}
❤️ Лайков сегодня: {N}
👀 Просмотров сегодня: {N}

📈 Регистраций за неделю: {N}
```

### 🎓 Университеты:
```
Сообщение: "🎓 Управление университетами"

Inline клавиатура:
[➕ Добавить университет]
[📋 Список университетов]
[◀️ Назад]
```

**Добавить университет:**
```
Сообщение: "Введи название университета.\n\nФормат: Полное название | Сокращение | Город\n\nПример: Московский государственный университет | МГУ | Москва"
```

**Список университетов:**
```
🎓 Университеты:

1. МГУ им. М.В. Ломоносова (Москва) - 156 анкет
2. МГТУ им. Н.Э. Баумана (Москва) - 89 анкет
3. НИУ ВШЭ (Москва) - 234 анкет
...

Inline клавиатура под каждым:
[✏️ Ред.] [🗑️ Удал.] [🔴/🟢 Актив.]
```

### 📋 Жалобы:
```
Сообщение: "📋 Жалобы (показаны новые)"

[Анкета пользователя, на которого пожаловались]

Жалоба от: @username (ID: 123456)
Причина: {причина}
Комментарий: {комментарий}
Дата: {дата}

Inline клавиатура:
[🚫 Забанить] [⚠️ Предупредить] [✅ Отклонить жалобу]
[◀️ Пред.] [{N}/{Total}] [След. ▶️]
```

### 🚫 Забанить пользователя:
```
Сообщение: "Введи Telegram ID или @username пользователя для бана:"

После ввода:
[Показать анкету пользователя]

Сообщение: "Забанить этого пользователя?"

Inline клавиатура:
[✅ Да, забанить] [❌ Отмена]
```

При бане пользователю приходит:
```
⚠️ Твоя анкета была заблокирована за нарушение правил.

Если считаешь, что это ошибка, обратись к администратору.
```

### 📣 Рассылка:
```
Сообщение: "Выбери тип рассылки:"

Inline клавиатура:
[Всем пользователям]
[Определённому университету]
[Только активным]
[◀️ Назад]
```

После выбора:
```
Сообщение: "Отправь сообщение для рассылки (можно с фото):"
```

Подтверждение:
```
Сообщение: "Рассылка будет отправлена {N} пользователям.\n\nПодтвердить?"

Inline клавиатура:
[✅ Отправить] [❌ Отмена]
```

Прогресс:
```
📣 Рассылка: {sent}/{total} ({percent}%)
```

### 👥 Пользователи:
```
Сообщение: "Поиск пользователей:"

Inline клавиатура:
[🔍 По ID/username]
[🎓 По университету]
[📅 Последние регистрации]
[◀️ Назад]
```

---

## ТЕКСТЫ И ЭМОДЗИ

Все тексты должны быть в отдельном файле `text_templates.py`:

```python
TEXTS = {
    "welcome": "Привет! 👋\n\nЯ бот для знакомств в твоём университете! 🎓\n\nЗдесь ты можешь найти новых друзей, единомышленников или даже свою любовь среди студентов твоего вуза 💕\n\nДавай создадим твою анкету!",
    
    "choose_university": "🎓 Выбери свой университет из списка:",
    
    "ask_name": "Как тебя зовут? 😊\n\nНапиши своё имя (как хочешь, чтобы тебя видели другие)",
    
    "ask_age": "Сколько тебе лет? 🎂\n\nНапиши число от 16 до 99",
    
    "invalid_age": "❌ Пожалуйста, введи корректный возраст (от 16 до 99)",
    
    "ask_gender": "Укажи свой пол:",
    
    "ask_looking_for": "Кого ты хочешь найти? 💕",
    
    "ask_photo": "📸 Отправь свои фото (от 1 до 3 штук)\n\nПервое фото будет главным в анкете.\n\nКогда закончишь, нажми кнопку ниже.",
    
    "photo_added": "✅ Фото {n} добавлено! (всего: {n}/3)",
    
    "max_photos": "Максимум фото загружено! Нажми 'Готово ✅'",
    
    "ask_bio": "Расскажи о себе ✨\n\nНапиши пару слов: чем увлекаешься, что любишь, какой ты человек.\n\nМаксимум 500 символов.",
    
    "confirm_profile": "Отлично! Вот твоя анкета 👇",
    
    "profile_confirmed": "🎉 Отлично! Твоя анкета создана!\n\nТеперь ты можешь начать поиск.",
    
    "main_menu": "Что хочешь сделать? 💜",
    
    "no_profiles": "🔍 Анкеты в твоём университете закончились!\n\nПопробуй зайти позже — здесь появляются новые люди каждый день 💫",
    
    "no_likes": "💔 Пока никто не оценил твою анкету.\n\nНо не расстраивайся — продолжай просматривать анкеты, и симпатии обязательно появятся! 💫",
    
    "no_matches": "💔 У тебя пока нет взаимных симпатий.\n\nПродолжай просматривать анкеты — твоя пара может быть следующей! 💫",
    
    "its_a_match": "🎉 Есть взаимная симпатия!",
    
    "profile_hidden": "😴 Твоя анкета скрыта.\n\nТебя больше не будут видеть другие пользователи, но ты по-прежнему можешь просматривать анкеты.",
    
    "profile_visible": "💫 Твоя анкета снова активна!\n\nТеперь другие пользователи смогут тебя видеть.",
    
    "someone_liked_you": "💌 Кто-то оценил твою анкету!\n\nЗайди в раздел 'Я нравлюсь', чтобы узнать кто 👀",
    
    "report_sent": "✅ Жалоба отправлена. Мы проверим эту анкету.",
    
    "banned": "⚠️ Твоя анкета была заблокирована за нарушение правил.\n\nЕсли считаешь, что это ошибка, обратись к администратору.",
    
    "views_reset": "🔄 Просмотренные анкеты сброшены!\n\nТеперь тебе снова будут показываться все анкеты твоего университета.",
    
    "write_message_prompt": "💌 Напиши сообщение для этого человека (до 200 символов):\n\nОно будет показано вместе с твоей анкетой.",
    
    "has_likes": "💌 Тебе нравятся {count} человек!\n\nСмотрим?",
    
    "matches_header": "💕 Твои взаимные симпатии ({count}):",
}
```

---

## КЛАВИАТУРЫ

### Reply клавиатуры:

```python
# Главное меню
main_menu_kb = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(text="1. Смотреть анкеты 👀"), KeyboardButton(text="2. Моя анкета 📝")],
        [KeyboardButton(text="3. Я нравлюсь 💌"), KeyboardButton(text="4. Взаимные 💕")],
        [KeyboardButton(text="5. Выключить анкету 😴")],  # или "Включить анкету 💫"
    ],
    resize_keyboard=True
)

# Выбор пола
gender_kb = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(text="Я парень 👨")],
        [KeyboardButton(text="Я девушка 👩")],
    ],
    resize_keyboard=True
)

# Кого ищем
looking_for_kb = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(text="Парней 👨")],
        [KeyboardButton(text="Девушек 👩")],
        [KeyboardButton(text="Без разницы 🤷")],
    ],
    resize_keyboard=True
)
```

### Inline клавиатуры:

```python
# Просмотр анкеты
def viewing_kb():
    return InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(text="❤️", callback_data="like"),
            InlineKeyboardButton(text="👎", callback_data="dislike"),
            InlineKeyboardButton(text="💌", callback_data="like_with_message"),
            InlineKeyboardButton(text="💤", callback_data="go_sleep"),
            InlineKeyboardButton(text="⚠️", callback_data="report"),
        ]
    ])

# Просмотр входящего лайка
def incoming_like_kb():
    return InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(text="❤️", callback_data="mutual_like"),
            InlineKeyboardButton(text="👎", callback_data="reject_like"),
            InlineKeyboardButton(text="💤", callback_data="go_sleep"),
        ]
    ])

# Университеты с пагинацией
def universities_kb(universities: list, page: int, total_pages: int):
    buttons = []
    for uni in universities:
        buttons.append([InlineKeyboardButton(
            text=uni.name, 
            callback_data=f"uni_{uni.id}"
        )])
    
    nav = []
    if page > 1:
        nav.append(InlineKeyboardButton(text="◀️", callback_data=f"uni_page_{page-1}"))
    if page < total_pages:
        nav.append(InlineKeyboardButton(text="▶️", callback_data=f"uni_page_{page+1}"))
    
    if nav:
        buttons.append(nav)
    
    return InlineKeyboardMarkup(inline_keyboard=buttons)
```

---

## ВАЖНЫЕ ДЕТАЛИ РЕАЛИЗАЦИИ

### 1. Подбор анкет (MatchingService):

```python
async def get_next_profile(self, user: User) -> Optional[User]:
    """Получить следующую анкету для просмотра"""
    
    # Получить ID уже просмотренных
    viewed_ids = await self.get_viewed_profile_ids(user.id)
    
    # Определить искомый пол
    if user.looking_for == "any":
        gender_filter = ["male", "female"]
    else:
        gender_filter = [user.looking_for]
    
    # Запрос
    query = select(User).where(
        User.university_id == user.university_id,  # Тот же университет
        User.id != user.id,  # Не сам себе
        User.id.not_in(viewed_ids),  # Не просмотренные
        User.is_active == True,
        User.is_banned == False,
        User.is_registered == True,
        User.show_in_search == True,
        User.gender.in_(gender_filter),
        # Взаимный интерес по полу
        or_(
            User.looking_for == "any",
            User.looking_for == user.gender
        )
    ).order_by(
        User.last_active.desc()  # Сначала активные
    ).limit(1)
    
    result = await session.execute(query)
    return result.scalar_one_or_none()
```

### 2. Проверка на взаимный лайк:

```python
async def check_mutual_like(self, from_user_id: int, to_user_id: int) -> bool:
    """Проверить, есть ли ответный лайк"""
    query = select(Like).where(
        Like.from_user_id == to_user_id,
        Like.to_user_id == from_user_id,
        Like.is_like == True
    )
    result = await session.execute(query)
    return result.scalar_one_or_none() is not None
```

### 3. Создание мэтча:

```python
async def create_match(self, user1_id: int, user2_id: int) -> Match:
    """Создать взаимную симпатию"""
    # Упорядочить ID чтобы избежать дубликатов
    first_id, second_id = sorted([user1_id, user2_id])
    
    match = Match(user1_id=first_id, user2_id=second_id)
    session.add(match)
    await session.commit()
    return match
```

### 4. Отправка медиагруппы (несколько фото):

```python
async def send_profile(self, chat_id: int, user: User, keyboard=None):
    """Отправить анкету пользователю"""
    caption = f"{user.name}, {user.age}, {user.university.short_name} 🎓\n\n{user.bio}"
    
    photos = [user.photo_1]
    if user.photo_2:
        photos.append(user.photo_2)
    if user.photo_3:
        photos.append(user.photo_3)
    
    if len(photos) == 1:
        await bot.send_photo(
            chat_id=chat_id,
            photo=photos[0],
            caption=caption,
            reply_markup=keyboard
        )
    else:
        media = [InputMediaPhoto(media=photos[0], caption=caption)]
        for photo in photos[1:]:
            media.append(InputMediaPhoto(media=photo))
        
        await bot.send_media_group(chat_id=chat_id, media=media)
        # Клавиатуру отправить отдельным сообщением
        if keyboard:
            await bot.send_message(chat_id=chat_id, text="⬆️", reply_markup=keyboard)
```

### 5. Middleware для сессии БД:

```python
class DbSessionMiddleware(BaseMiddleware):
    def __init__(self, session_factory: async_sessionmaker):
        self.session_factory = session_factory

    async def __call__(
        self,
        handler: Callable,
        event: TelegramObject,
        data: Dict[str, Any]
    ) -> Any:
        async with self.session_factory() as session:
            data["session"] = session
            return await handler(event, data)
```

### 6. Обновление last_active:

При каждом действии пользователя обновлять поле `last_active` для корректной сортировки.

---

## .env ФАЙЛ

```
BOT_TOKEN=your_telegram_bot_token
ADMIN_ID=123456789
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dating_bot
```

## requirements.txt

```
aiogram==3.4.1
asyncpg==0.29.0
SQLAlchemy==2.0.25
alembic==1.13.1
python-dotenv==1.0.0
aiohttp==3.9.1
```

---

## ДОПОЛНИТЕЛЬНЫЕ ТРЕБОВАНИЯ

1. **Все тексты на русском языке**
2. **Использовать эмодзи везде где уместно**
3. **Обработка всех возможных ошибок с понятными сообщениями**
4. **Логирование важных действий**
5. **Graceful shutdown**
6. **Валидация всех пользовательских вводов**
7. **Защита от спама (rate limiting)**
8. **Callback query answer() для всех inline кнопок**
9. **Удаление предыдущих сообщений при переходах для чистоты чата**

---

Создай полностью рабочего бота с всеми указанными функциями. Код должен быть чистым, хорошо структурированным и готовым к продакшену.
```

---

Этот промпт максимально детально описывает весь функционал.